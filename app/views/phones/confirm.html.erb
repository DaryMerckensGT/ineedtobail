<%= simple_form_for(resource) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :user_confirmation, label: "Enter your confirmation code:" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Confirm Phone" %>
  </div>
<% end %>